import { Circle } from "./circle";

const circle = new Circle(10);

console.log(circle.show());
console.log(circle.show());
console.log(circle.show());
console.log(circle.show());
console.log(circle.show());
console.log(circle.show());
