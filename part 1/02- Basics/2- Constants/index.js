// 1. Declare a constant: if a variable doesn't need to be reassigned, it should be declared with 'const'.
// Otherwise, it should be a variable declared with 'let'.
const interestRate = 0.3;

// The following line is commented out because it would cause an error: Constants cannot be reassigned.
// interestRate = 1;
console.log(interestRate);
