/// 1. In JavaScript, memory allocation and deallocation are managed automatically.
///    - Memory allocation occurs when variables or objects are created.
///    - Memory deallocation happens via garbage collection when objects are no longer referenced.

let circle = {}; // Allocates memory for an empty object
console.log(circle);
