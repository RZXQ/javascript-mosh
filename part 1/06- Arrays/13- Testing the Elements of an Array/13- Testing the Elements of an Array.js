const numbers = [1, -1, 2, 3];

// ==================== 1. Using `every()` ====================
console.log(numbers.every((n) => n >= 0));

// ==================== 2. Using `some()` ====================
console.log(numbers.some((n) => n >= 0));
