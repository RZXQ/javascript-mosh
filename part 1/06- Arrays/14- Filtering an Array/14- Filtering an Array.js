// ===========================================================
//                        `filter()`
// ===========================================================

const numbers = [1, -1, 2, 3];
console.log(numbers.filter((n) => n >= 0));
